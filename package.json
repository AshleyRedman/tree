{
    "name": "graph",
    "module": "index.ts",
    "type": "module",
    "scripts": {
        "types": "tsc --noEmit",
        "lint": "biome lint .",
        "format": "biome check . --write",
        "check": "biome ci . && pnpm run types"
    },
    "devDependencies": {
        "@biomejs/biome": "1.8.1",
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
